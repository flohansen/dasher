apiVersion: v1
kind: Service
metadata:
  name: {{ include "dasher.fullname" . }}
spec:
  selector:
    app: dasher-server
  ports:
    - name: web
      port: 3000
      protocol: TCP
    - name: grpc
      port: 50051
      protocol: TCP
